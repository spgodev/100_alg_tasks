package recursion

// SumAtDepth возвращает сумму Value всех узлов на глубине depth.
// root имеет глубину 0.
// Если root == nil или depth < 0 — вернуть 0.
//
// Пример:
//      1          depth 0 -> 1
//    / | \
//   2  3  4       depth 1 -> 2 + 3 + 4 = 9

func SumAtDepth(root *TreeNode, depth int) int {
	// TODO
	return 0
}
